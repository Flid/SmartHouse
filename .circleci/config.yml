version: 2
jobs:
  build:
    machine:
      enabled: true

    working_directory: ~/smart_house

    steps:
      - checkout
      - run:
          name: Build prerequisites
          command: |
            echo "$CIRCLE_SHA1" > .commit-id
            docker --version
            docker login -u "$DOCKER_USER" -p "$DOCKER_PASS"
      - run:
          name: Builing container
          command: |
            cp -r qemu testapp/
            docker build -t antonkir/testapp:$(git rev-parse HEAD) ./testapp
      - deploy:
          command: |
            docker push antonkir/testapp:$(git rev-parse HEAD)
