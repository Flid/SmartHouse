# -*- coding: utf-8 -*-

from smart_house.core import validators

'''
class LoginOccupationForm(validators.Schema):
    login = validators.String(if_missing=None)


class CreateUserDefinedGoalForm(validators.Schema):
    category_id = validators.Int(strip=True, not_empty=True)
    description = validators.String(strip=True, not_empty=True)
    week_days = validators.String(strip=True, not_empty=True)
    single_score = validators.Int(strip=True, not_empty=True)
    single_score_penalty = validators.Int(strip=True, not_empty=True)
    schema_type = validators.String(strip=True, not_empty=True)
    schema_target_days = validators.Int(strip=True, not_empty=True)


class CreateActiveGoalForm(validators.Schema):
    goal_id = validators.Int(strip=True, not_empty=True)
'''
